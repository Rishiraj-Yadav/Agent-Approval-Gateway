# Architecture

The canonical architecture document is **[docs/architecture.md](docs/architecture.md)**
(product overview, components, data flow, adapters, Telegram, local gateway,
relay, database, authN/authZ, lifecycle, state machine, security boundaries,
failure handling, concurrency, deployment, extension model, future web-UI
boundary).

This root file is a pointer kept so `architecture.md` resolves from the repo
root; it intentionally duplicates nothing so the two cannot drift.
