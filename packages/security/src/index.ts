/**
 * @raag/security — Redaction and secret-handling primitives (patterns applied before anything leaves the process toward Telegram, logs, or storage).
 *
 * Phase 1 scaffold: placeholders only. Implementation per
 * docs/architecture.md lands in Phase 2+.
 */
export const packageName = '@raag/security' as const;
