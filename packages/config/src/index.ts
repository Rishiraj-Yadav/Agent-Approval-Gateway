/**
 * @raag/config — Config loading and validation: defaults, env-var sources, and startup invariants (loopback-only bind, minimum key entropy).
 *
 * Phase 1 scaffold: placeholders only. Implementation per
 * docs/architecture.md lands in Phase 2+.
 */
export const packageName = '@raag/config' as const;
