/**
 * @raag/domain — Shared types, port interfaces, and the approval state machine. Zero external and zero workspace dependencies — everything may import this, it imports nothing.
 *
 * Phase 1 scaffold: placeholders only. Implementation per
 * docs/architecture.md lands in Phase 2+.
 */
export const packageName = '@raag/domain' as const;
